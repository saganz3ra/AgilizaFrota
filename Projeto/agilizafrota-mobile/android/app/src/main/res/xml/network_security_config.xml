<?xml version="1.0" encoding="utf-8"?>
<!--
  Politica de rede (RNF01).

  Por padrao o Android bloqueia HTTP sem TLS. Mantemos esse bloqueio para
  TODO o restante da internet e abrimos excecao apenas para o servidor
  local de desenvolvimento. Em producao a API roda sob HTTPS e este
  arquivo pode ser reduzido ao bloco de base-config.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <!-- Maquina de desenvolvimento na rede local -->
        <domain includeSubdomains="false">192.168.1.9</domain>
        <!-- Emulador Android (host da maquina) -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
